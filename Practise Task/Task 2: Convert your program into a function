#Task 2: Convert your program into a function
#Requirements:
#Create a function called check_eligibility
#It should take name and age as parameters
#It should print eligibility result
#Call the function from main program

def check_eligiblity( name, age):
    voting_age = 18
    if age >= voting_age:
        print(f"Hello, {name}! Your are eligible for vote.")
    else:
        years_to_vote = voting_age-age
        print(f"Sorry, {name}. You are only {age} years old. You will be eligible in {years_to_vote}")

name = input("Enter your name: ")
age = int(input("Enter your age: "))
check_eligiblity(name, age)
